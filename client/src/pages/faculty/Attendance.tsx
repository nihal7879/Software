import { AttendanceInbox } from '../../components/AttendanceInbox';

/** The teacher's own scanned check-ins, waiting to be confirmed into lectures. */
export default function FacultyAttendance() {
  return <AttendanceInbox />;
}
